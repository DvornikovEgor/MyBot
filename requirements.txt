# Веб-сервер и интерфейс
flask>=3.0

# Офлайн-движок: нейросеть Silero (русские голоса)
numpy>=1.24,<3
# На машинах без видеокарты NVIDIA удобнее ставить CPU-сборку:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Облачный движок: сотни голосов на разных языках (нужен интернет)
edge-tts>=6.1
